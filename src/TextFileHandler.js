import {retext} from 'retext'
import retextPos from 'retext-pos'
import retextKeywords from 'retext-keywords'

export const TextFileHandler = (fileName, path) => {

    console.log(fileName, path);

    let textData;

    return textData;

}